apiVersion: apps/v1
kind: Deployment
metadata:
  name: hct-frontend
  namespace: hct-huaripaucar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hct-frontend
  template:
    metadata:
      labels:
        app: hct-frontend
    spec:
      containers:
        - name: hct-frontend
          image: jesushuaripaucarcardenas/hct-frontend:latest
          ports:
            - containerPort: 80
